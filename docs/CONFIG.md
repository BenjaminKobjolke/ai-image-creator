# JSON Config Reference

The batch input file is a **JSON list** of request objects. Each object is one
image request, validated at the boundary by `ai_image_creator/models.py`
(`ImageRequest` / `OutputSpec`, Pydantic, `extra="forbid"` — unknown keys are
rejected). CLI flags map 1:1 to these fields for single-request mode.

```json
[
  {
    "prompt": "a red fox in deep snow",
    "provider": "openai",
    "model": "gpt-image-1",
    "size": "1024x1024",
    "n": 1,
    "quality": "high",
    "output": {
      "path": "output/fox.png",
      "width": 800,
      "height": 600,
      "crop": "center",
      "format": "png"
    }
  }
]
```

## Request fields

| Field      | Type    | Required | Default        | Notes |
|------------|---------|----------|----------------|-------|
| `prompt`   | string  | yes      | —              | Non-empty (min length 1). |
| `provider` | string  | no       | `openai`       | `openai` or `gemini`. |
| `model`    | string  | no       | `gpt-image-2`  | Must be a known model (see below). Not checked against `provider`. |
| `size`     | string  | no       | model default  | Must be valid for the model, else rejected. OpenAI = pixels (`1024x1024`); Gemini = aspect ratio (`16:9`). |
| `n`        | integer | no       | `1`            | Images per request; `>= 1`. |
| `quality`  | string  | no       | provider default | OpenAI only (e.g. `high` for gpt-image-1). Ignored by Gemini. |
| `output`   | object  | no       | save raw as-is | Post-processing block (below). |

### `provider` × `model`

`size` is validated against the **model**, not the provider — there is no check
that the model belongs to the selected provider, so pair them correctly yourself.

| provider | models |
|----------|--------|
| `openai` | `gpt-image-2` (default), `gpt-image-1`, `dall-e-3`, `dall-e-2` |
| `gemini` | `gemini-2.5-flash-image`, `imagen-4.0-generate-001`, `imagen-4.0-ultra-generate-001`, `imagen-4.0-fast-generate-001` |

Valid `size` values per model: [models/OPEN_AI.md](models/OPEN_AI.md) and
[models/GEMINI.md](models/GEMINI.md).

## `output` block

Optional. Omit it to save the raw generated bytes as-is. When present, the image is
resized/cropped with Pillow after generation (independent of the provider).

| Field    | Type    | Required | Default  | Notes |
|----------|---------|----------|----------|-------|
| `path`   | string  | yes      | —        | Output file path. |
| `width`  | integer | no       | —        | Positive; must be set together with `height`. |
| `height` | integer | no       | —        | Positive; must be set together with `width`. |
| `crop`   | string  | no       | `center` | `center`, `top`, `bottom`, `left`, `right`, `stretch`. |
| `format` | string  | no       | `png`    | e.g. `png`, `jpg`. |

`width`+`height` must be set together (validation fails otherwise). When `n > 1`,
an index is inserted into the filename (`fox_0.png`, `fox_1.png`, …).

Two size concepts, kept separate: `size` = what the provider generates; `output` =
the final file on disk (any width/height + crop).
